package functionalPrograms;

public class Sum3EqualtoZero extends Utility{
void process() {
	System.out.println("Enter the Size of an array");
	int size=getValue();
	int array[]= getArray(size);   
	printArray(array, size);
   
}
}
